version: 1.0
runtime: python3.10
build:
  commands:
    - python -m pip install --upgrade pip
    - python -m pip install -r requirements.txt
startCommand: uvicorn sentiment:app --host 0.0.0.0 --port 8080
port: 8080